import {Component} from "angular2/core";
import {bootstrap} from "angular2/browser/platform";

@Component({
  selector: 'app',
  template: `
    <h2>Component Loaded</h2>
  `
})

class App(

)

bootstrap(App);
